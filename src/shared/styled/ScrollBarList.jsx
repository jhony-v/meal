import styled from "@emotion/styled";

const ScrollBarList = styled.div`
    width:100%;
    overflow-x:auto;
    white-space:nowrap;
    > div {
        display:inline-block;
    }
`
export default ScrollBarList;